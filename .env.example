# No external APIs or secret keys are required.
# The app is 100% client-side static, hosted with zero configuration on GitHub Pages and Vercel.
# Leaflet and OpenStreetMap tiles require zero API keys.

